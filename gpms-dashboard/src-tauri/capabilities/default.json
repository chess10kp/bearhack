{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "gpms-dashboard-default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-app-commands"
  ]
}
