[[permission]]
identifier = "allow-app-commands"
description = "Allow GPMS dashboard shell, system info, and config file read."
commands.allow = [
  "execute_command",
  "get_system_info",
  "read_config",
]
